# 🔍 玩家搜索功能使用指南

## 📋 功能概述

玩家列表页面提供了强大的多条件搜索功能，支持以下搜索条件的任意组合。

---

## 🔎 搜索条件说明

### 1. 用户ID（精确查询）
- **字段名**: `userId`
- **类型**: Long
- **查询方式**: 精确匹配
- **示例**: `2859435`
- **说明**: 根据用户的唯一ID进行精确查找

### 2. 登录名/用户名（模糊查询）
- **字段名**: `username`
- **类型**: String
- **查询方式**: 模糊匹配（LIKE）
- **示例**: `"正常用户"` 或 `"test"`
- **说明**: 可以输入用户名的一部分进行搜索

### 3. 手机号（模糊查询）
- **字段名**: `phone`
- **类型**: String
- **查询方式**: 模糊匹配（LIKE）
- **示例**: `"138"` 或 `"125830572"`
- **说明**: 可以输入手机号的一部分进行搜索

### 4. 邮箱（模糊查询）
- **字段名**: `email`
- **类型**: String
- **查询方式**: 模糊匹配（LIKE）
- **示例**: `"@gmail"` 或 `"test@example.com"`
- **说明**: 可以输入邮箱的一部分进行搜索

### 5. 用户类型（精确查询）
- **字段名**: `userType`
- **类型**: Integer
- **查询方式**: 精确匹配
- **可选值**:
  - `0` - 普通用户
  - `1` - 管理员
- **默认值**: 如果不指定，默认只查询普通用户（0）
- **说明**: 选择要查询的用户类型

### 6. 地址/钱包地址（模糊查询）
- **字段名**: `walletAddress`
- **类型**: String
- **查询方式**: 模糊匹配（LIKE）
- **示例**: `"0x"` 或 `"无地址"`
- **说明**: 根据用户的钱包地址进行搜索

### 7. 冻结状态（精确查询）
- **字段名**: `status`
- **类型**: Integer
- **查询方式**: 精确匹配
- **可选值**:
  - `0` - 禁用/冻结
  - `1` - 正常
- **说明**: 筛选特定状态的用户

### 8. 玩家代理（精确查询）
- **字段名**: `agentId`
- **类型**: Long
- **查询方式**: 精确匹配
- **示例**: `2850417`
- **说明**: 查找指定代理下的所有玩家

### 9. 注册域名（模糊查询）
- **字段名**: `registerDomain`
- **类型**: String
- **查询方式**: 模糊匹配（LIKE）
- **示例**: `"example.com"`
- **说明**: 根据用户注册时的域名进行搜索

---

## 🌐 API 调用示例

### 基础查询（只分页）
```bash
GET /admin/player/list?page=1&size=10
```

### 按用户ID查询
```bash
GET /admin/player/list?userId=2859435
```

### 按用户名模糊查询
```bash
GET /admin/player/list?username=正常用户
```

### 按手机号模糊查询
```bash
GET /admin/player/list?phone=138
```

### 按状态查询（只查询正常用户）
```bash
GET /admin/player/list?status=1
```

### 按代理ID查询
```bash
GET /admin/player/list?agentId=2850417
```

### 组合查询示例
```bash
# 查询状态正常且手机号包含138的用户
GET /admin/player/list?status=1&phone=138

# 查询指定代理下的所有正常用户
GET /admin/player/list?agentId=2850417&status=1

# 查询用户名包含"test"且邮箱包含"gmail"的用户
GET /admin/player/list?username=test&email=gmail
```

---

## 💻 前端调用示例

### JavaScript/Axios 示例

```javascript
// 基础查询
axios.get('/admin/player/list', {
  params: {
    page: 1,
    size: 10
  }
})

// 多条件搜索
axios.get('/admin/player/list', {
  params: {
    page: 1,
    size: 10,
    username: '正常用户',
    status: 1,
    phone: '138'
  }
})

// 搜索表单提交
function searchPlayers(searchForm) {
  const params = {
    page: 1,
    size: 10
  };
  
  // 只添加有值的搜索条件
  if (searchForm.userId) params.userId = searchForm.userId;
  if (searchForm.username) params.username = searchForm.username;
  if (searchForm.phone) params.phone = searchForm.phone;
  if (searchForm.email) params.email = searchForm.email;
  if (searchForm.userType !== null) params.userType = searchForm.userType;
  if (searchForm.walletAddress) params.walletAddress = searchForm.walletAddress;
  if (searchForm.status !== null) params.status = searchForm.status;
  if (searchForm.agentId) params.agentId = searchForm.agentId;
  if (searchForm.registerDomain) params.registerDomain = searchForm.registerDomain;
  
  return axios.get('/admin/player/list', { params });
}
```

---

## 📊 响应数据格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 2859435,
        "username": "正常用户",
        "phone": "125830572",
        "email": null,
        "userType": 0,
        "walletAddress": "无地址",
        "status": 1,
        "agentId": 2850417,
        "registerDomain": null,
        "vipLevel": 0,
        "kycLevel": 0,
        "uid": "UID100001",
        "inviteCode": "INVITE001",
        "createTime": "2025-12-31 01:11:33"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

---

## ⚠️ 注意事项

1. **所有搜索条件都是可选的**
   - 可以不传任何搜索条件，只进行分页查询
   - 可以传一个或多个搜索条件进行组合查询

2. **默认行为**
   - 如果不指定 `userType`，默认只查询普通用户（userType = 0）
   - 如果要查询所有类型用户，需要明确传递 `userType` 参数

3. **模糊查询**
   - 用户名、手机号、邮箱、钱包地址、注册域名都支持模糊查询
   - 输入部分内容即可匹配

4. **精确查询**
   - 用户ID、用户类型、状态、代理ID 都是精确匹配
   - 必须输入完整准确的值

5. **分页参数**
   - `page` 默认值为 1
   - `size` 默认值为 10
   - 可以根据需要调整每页显示数量

---

## 🎯 使用场景

### 场景 1：查找特定用户
```
输入用户ID: 2859435
→ 快速定位到该用户
```

### 场景 2：查找某个代理的所有下级
```
输入代理ID: 2850417
→ 显示该代理的所有下级玩家
```

### 场景 3：查找被冻结的用户
```
选择冻结状态: 0（禁用）
→ 显示所有被冻结的用户
```

### 场景 4：查找特定域名注册的用户
```
输入注册域名: example.com
→ 显示从该域名注册的所有用户
```

### 场景 5：组合查询
```
输入用户名: test
选择状态: 1（正常）
输入手机号: 138
→ 显示用户名包含"test"、状态正常、手机号包含"138"的用户
```

---

## 🔐 权限要求

- **接口**: `GET /admin/player/list`
- **权限**: `ADMIN`
- **说明**: 只有管理员可以访问此接口

---

## 🚀 测试建议

1. 使用 Swagger UI 测试：`http://localhost:8080/doc.html`
2. 测试单个搜索条件
3. 测试多个搜索条件组合
4. 验证分页功能
5. 检查返回数据的准确性

