# 🧪 后台管理系统测试指南

## 📋 测试前准备

### 1. 启动应用

在 IntelliJ IDEA 中运行 `CryptoPlatformApplication.java`

### 2. 确认应用启动成功

查看控制台输出，应该看到：
```
Started CryptoPlatformApplication in X.XXX seconds
```

### 3. 访问 Swagger 文档

打开浏览器访问: http://localhost:8080/api/doc.html

---

## 🎯 测试方法

### 方法 1: 使用 Swagger UI（推荐）

1. 访问 http://localhost:8080/api/doc.html
2. 找到对应的接口分组
3. 点击接口
4. 点击"试一试"按钮
5. 填写参数（如果需要）
6. 点击"发送"
7. 查看响应结果

### 方法 2: 使用测试脚本

```bash
# 给脚本添加执行权限
chmod +x test-admin-apis.sh

# 运行测试脚本
./test-admin-apis.sh
```

### 方法 3: 使用 curl 命令

```bash
# 首页统计
curl http://localhost:8080/api/admin/dashboard/statistics

# 平台概要
curl http://localhost:8080/api/admin/platform/overview

# 玩家详情
curl http://localhost:8080/api/admin/player/1
```

---

## ✅ 测试清单

### 1. 首页数据统计模块

- [ ] GET /api/admin/dashboard/statistics
  - 预期结果：返回平台总收入、玩家数量、充值/提现金额、趋势数据
  - 验证点：totalRevenue、playerCount、depositTrend、withdrawTrend

### 2. 平台数据模块

- [ ] GET /api/admin/platform/overview
  - 预期结果：返回平台概要信息
  - 验证点：totalUsers、todayNewUsers、vipUserCount

- [ ] GET /api/admin/platform/daily-data
  - 预期结果：返回每日数据分页列表
  - 验证点：分页参数、日期筛选

- [ ] GET /api/admin/platform/player-data
  - 预期结果：返回玩家数据分页列表
  - 验证点：搜索、VIP筛选、状态筛选

### 3. 玩家管理模块

- [ ] GET /api/admin/player/{userId}
  - 预期结果：返回玩家详细信息
  - 验证点：基本信息、资产列表、统计数据

- [ ] PUT /api/admin/player/{userId}/status
  - 预期结果：更新玩家状态成功
  - 验证点：status 参数

- [ ] PUT /api/admin/player/{userId}/vip-level
  - 预期结果：设置VIP等级成功
  - 验证点：vipLevel 参数

### 4. 杂项管理模块 - 秒合约

- [ ] GET /api/admin/misc/seconds-contract/orders
  - 预期结果：返回秒合约订单列表
  - 验证点：分页、筛选参数

- [ ] GET /api/admin/misc/seconds-contract/orders/{id}
  - 预期结果：返回订单详情

### 5. 杂项管理模块 - 周期合约

- [ ] GET /api/admin/misc/cycle-contract/orders
  - 预期结果：返回周期合约订单列表
  - 验证点：分页、筛选参数

- [ ] GET /api/admin/misc/cycle-contract/orders/{id}
  - 预期结果：返回订单详情

### 6. 杂项管理模块 - IEO项目

- [ ] GET /api/admin/misc/ieo/projects
  - 预期结果：返回IEO项目列表
  - 验证点：分页、状态筛选

- [ ] GET /api/admin/misc/ieo/projects/{id}
  - 预期结果：返回项目详情

- [ ] POST /api/admin/misc/ieo/projects
  - 预期结果：创建项目成功

- [ ] PUT /api/admin/misc/ieo/projects/{id}
  - 预期结果：更新项目成功

- [ ] DELETE /api/admin/misc/ieo/projects/{id}
  - 预期结果：删除项目成功

### 7. 杂项管理模块 - 现货交易

- [ ] GET /api/admin/misc/spot/orders
  - 预期结果：返回现货交易订单列表
  - 验证点：分页、筛选参数

- [ ] GET /api/admin/misc/spot/orders/{id}
  - 预期结果：返回订单详情

---

## 🔍 常见问题

### 1. 应用无法启动

**问题**: 启动时报错

**解决方案**:
- 检查 MySQL 是否运行
- 检查数据库连接配置
- 查看完整错误堆栈

### 2. API 返回 404

**问题**: 接口找不到

**解决方案**:
- 确认应用已启动
- 检查 URL 是否正确
- 确认 context-path 是 `/api`

### 3. API 返回空数据

**问题**: 接口返回成功但数据为空

**解决方案**:
- 检查数据库是否有测试数据
- 运行 `init.sql` 插入测试数据

### 4. 数据库连接失败

**问题**: 无法连接到 MySQL

**解决方案**:
- 确认 MySQL 服务已启动
- 检查用户名密码是否正确
- 确认数据库 `crypto_platform` 已创建

---

## 📊 性能测试

### 使用 Apache Bench (ab)

```bash
# 测试首页统计接口
ab -n 1000 -c 10 http://localhost:8080/api/admin/dashboard/statistics

# 测试玩家数据列表接口
ab -n 1000 -c 10 "http://localhost:8080/api/admin/platform/player-data?current=1&size=10"
```

---

## 📝 测试报告模板

```
测试日期: 2025-12-31
测试人员: [你的名字]
应用版本: 1.0.0

测试结果:
- 首页数据统计: ✅ 通过
- 平台数据模块: ✅ 通过
- 玩家管理模块: ✅ 通过
- 杂项管理模块: ✅ 通过

发现问题:
1. [问题描述]
2. [问题描述]

建议:
1. [优化建议]
2. [优化建议]
```

---

## 🔗 相关链接

- Swagger 文档: http://localhost:8080/api/doc.html
- Druid 监控: http://localhost:8080/api/druid/
- API 文档: BACKEND_ADMIN_API.md
- 开发总结: DEVELOPMENT_SUMMARY.md

